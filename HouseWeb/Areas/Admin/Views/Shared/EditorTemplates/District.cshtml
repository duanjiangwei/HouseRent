@using HouseBLL;
@model houseRent.District
@{
    var data = new DistrictService().GetAll();
    var list = new SelectList(data, "Id", "Name");
}
@Html.DropDownListFor(m => m.Id, list, "--请选择--",new { @class="selector"}))